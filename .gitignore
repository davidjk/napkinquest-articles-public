# build output
dist/
.output/
.vercel/
.netlify/

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.local
.env.production
.env.development
.env.*
!.env.example

# editor
.idea/
.vscode/

# os
.DS_Store
Thumbs.db

# bun
bun.lockb
.bun-cache/

# pagefind cache
pagefind/

# misc
*.log

# Agent / session guidance — kept local, not tracked
CLAUDE.md

# In-progress drafts (markdown + supporting assets) — staged here until published.
# Preview by symlinking the .md into posts/; symlinks show up in `git status`
# so they're easy to spot, but never `git add` them — publish via `mv` instead.
drafts/
